# =============================================
# Munim Backend — Python Dependencies
# Python 3.11+ required
# =============================================

# --- Web Framework ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.18        # File upload support

# --- Data Validation ---
pydantic==2.10.4
pydantic-settings==2.7.0

# --- Database (Neon PostgreSQL) ---
asyncpg==0.30.0                 # Async PostgreSQL driver
sqlalchemy[asyncio]==2.0.36     # ORM + query builder

# --- Data Pipeline ---
pandas==2.2.3
polars==1.19.0                  # For large files >50MB (10x faster than pandas)
numpy==2.2.1
openpyxl==3.1.5                 # Excel .xlsx parsing
xlrd==2.0.1                     # Excel .xls (legacy) parsing
lxml==5.3.0                     # Tally XML parsing

# --- Data Cleaning ---
rapidfuzz==3.11.0               # Fuzzy string matching for product deduplication
python-dateutil==2.9.0.post0    # Smart date parsing

# --- Analytics & ML ---
scikit-learn==1.6.1             # IsolationForest, preprocessing
prophet==1.1.6                  # Time series forecasting (v2)
lightgbm==4.5.0                 # Revenue forecasting (v2)

# --- LLM (Google Gemini 2.0 Flash — Free tier) ---
google-generativeai==0.8.3

# --- WhatsApp + Conversation ---
langgraph==0.2.60               # Stateful Q&A conversation engine
httpx==0.28.1                   # Async HTTP client for Meta API calls

# --- Task Queue ---
celery==5.4.0
redis==5.2.1                    # Upstash Redis client

# --- File Storage (Cloudflare R2 — S3 compatible) ---
boto3==1.35.88

# --- Auth ---
python-jose[cryptography]==3.3.0  # JWT validation
passlib[bcrypt]==1.7.4

# --- Monitoring ---
sentry-sdk[fastapi]==2.19.2

# --- Security ---
slowapi==0.1.9                  # Rate limiting for FastAPI

# --- Email ---
resend==2.5.0

# --- Dev / Testing ---
pytest==8.3.4
pytest-asyncio==0.25.1
pytest-cov==6.0.0
httpx==0.28.1                   # Also used for TestClient
factory-boy==3.3.1              # Test data factories
freezegun==1.5.1                # Freeze time in tests
ruff==0.9.2                     # Linting
mypy==1.14.1                    # Type checking
